<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('head') %>
  </head>
<body>
  <header>
    <%- include('header') %>
    <h2><% if (isNew) { %>new post<% } else { %><a href="/">/post</a>/<%- slug %> &mdash; edit<% } %></h2>
    <hr>
  </header>
  <main>
    <% if (error) { %>
      <p class="form-error"><%= error %></p>
    <% } %>
    <form method="POST" action="<% if (isNew) { %>/admin/post/new<% } else { %>/admin/post/<%= slug %>/edit<% } %>">
      <% if (isNew) { %>
        <label for="slug">slug (lowercase letters, digits, hyphens)</label><br>
        <input type="text" id="slug" name="slug" value="<%= slug %>" pattern="[a-z0-9-]+" required><br><br>
      <% } %>
      <label for="rawContent">content</label><br>
      <textarea id="rawContent" name="rawContent" rows="30"><%= rawContent %></textarea><br><br>
      <button type="submit"><% if (isNew) { %>create<% } else { %>save<% } %></button>
      <% if (!isNew) { %>
        &nbsp;<a href="/post/<%= slug %>">cancel</a>
      <% } %>
    </form>
  </main>
</body>
</html>
